name: build

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

# A second push to a PR branch makes the first run's answer worthless, and the
# abandoned run still holds a runner — on the macOS ones, a queue slot the next
# PR then waits behind.
#
# Only a PR run is ever in a shared group. Everything else keys on the commit,
# so it gets a group of its own: putting main pushes in one group would
# SERIALIZE them even with cancelling off, and GitHub cancels a *pending* run
# when a newer one joins its group — so three merges inside one build would
# leave the middle commit with no run at all. That is the tick this is
# supposed to protect.
concurrency:
  group: build-${{ github.event_name == 'pull_request' && github.ref || github.sha }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  DEVELOPER_DIR: /Applications/Xcode_26.2.app/Contents/Developer

jobs:
  # Two macOS jobs, cut at the one boundary the steps actually have: a
  # `-derivedDataPath`. `Test` and `Analyze` share a Debug DerivedData —
  # `Analyze` is ~8s only because it reads what `Test` already built, so it
  # must stay below it — while `Release build` and the three guards on the
  # bundle share a Release one and reuse nothing from Debug. Nothing crosses,
  # so neither job `needs:` the other and they run side by side.
  #
  # The sources compiled twice inside one job before this: 70s of Debug in
  # `Test`, then 51s of Release from nothing. That job was 182s over eight PR
  # runs and it was the gate.
  #
  # Run both ways, seven pairs alternating a pre-split ref with this one so
  # both arms saw the same runners: the split takes a median 60s off the gate
  # (0-99s), and `Debug tests and analyze` was the gate in all seven. The
  # spread is the runner, not the split — `Test` alone ran 59-147s across those
  # fourteen runs with no source change — which is why the figure comes from
  # comparing the two shapes inside each run rather than across arms.
  #
  # The third macOS runner this buys does NOT queue behind the other two:
  # 6.4s mean to start across 21 macOS jobs here against 7.4s across 14 before,
  # which was the one thing that could have made it a loss.
  #
  # ⚠️ It is worth nothing on a run where the iOS companion is already the
  # pole. That happened once in seven, and it is the ceiling on this split:
  # nothing further cut out of the Debug half can go below that job.
  test:
    name: Debug tests and analyze
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - name: Show toolchain
        run: xcodebuild -version && swift --version
      # 99.6s mean of the 182s job this was cut out of, and 70s of that is one
      # Debug build of the target graph — 21s is `xcodebuild` bringing the test
      # host up before the first assertion, and 6s is the tests running. This
      # job is the gate, measured: it held the title in all seven post-split
      # runs, by 1-94s over whichever of the other two came second.
      - name: Test
        run: |
          xcodebuild \
            -project Perch.xcodeproj \
            -scheme Perch \
            -configuration Debug \
            -destination 'platform=macOS,arch=arm64' \
            -derivedDataPath DerivedData \
            CODE_SIGNING_ALLOWED=NO \
            test
      # MUST stay in this job and below `Test`: same `-derivedDataPath`, and
      # the whole reason it is ~8s rather than another Debug build.
      - name: Analyze
        run: |
          xcodebuild \
            -project Perch.xcodeproj \
            -scheme Perch \
            -configuration Debug \
            -destination 'platform=macOS,arch=arm64' \
            -derivedDataPath DerivedData \
            CODE_SIGNING_ALLOWED=NO \
            analyze

  # The Release half: one build and the three things that read the bundle it
  # leaves in DerivedData. ~72s, so it clears a macOS job's fixed cost several
  # times over — and every step here needs a Release `Perch.app`, which is what
  # makes it a group rather than a spread.
  release-build:
    name: Release build and bundle guards
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - name: Show toolchain
        run: xcodebuild -version && swift --version
      - name: Release build
        run: |
          xcodebuild \
            -project Perch.xcodeproj \
            -scheme Perch \
            -configuration Release \
            -destination 'platform=macOS,arch=arm64' \
            -derivedDataPath DerivedData \
            ARCHS=arm64 \
            ONLY_ACTIVE_ARCH=NO \
            CODE_SIGNING_ALLOWED=NO \
            build

      # The same slice guard the release runs, so the arm64 invariant is proven
      # on every PR rather than only on a tag — where a red run costs a retag.
      - name: The bundle is arm64, and only arm64
        run: scripts/assert-arm64-only.sh DerivedData/Build/Products/Release/Perch.app

      # ARCHS above is pinned to mirror the release build, which is a
      # different concern from the one this paragraph is about.
      #
      # Deliberately NOT passing ENABLE_CODE_COVERAGE=NO to the build above:
      # the point is to prove the project-level setting still holds on a stock
      # `build`. Xcode's default is YES and it leaks past `test` into `build`,
      # an instrumented binary behaves identically, and the only symptom is a
      # `default.profraw` appearing in whatever directory `perch` was run from.
      # AGENTS.md has the why; this is the thing that notices.
      #
      # "Leaks past `test`" is about the project setting, not about this job:
      # `test` now runs on another runner entirely, so what this proves is that
      # a stock Release `build` is clean on its own.
      - name: Built binaries carry no profiling instrumentation
        run: |
          scripts/assert-no-instrumentation.sh \
            DerivedData/Build/Products/Release/Perch.app

      # `skill` and `doctor` are the only verbs that answer with no app running,
      # which is what makes them checkable here — and what lets them rot: no
      # other test in the suite compiles PerchCLI at all. This pins the bytes
      # `skill` prints and the exit codes both verbs promise in docs/cli.md,
      # ai/SKILL.md and --help, against the binary that would actually ship.
      - name: The shell-free verbs answer as documented
        run: |
          scripts/check-cli-surface.sh \
            DerivedData/Build/Products/Release/Perch.app/Contents/MacOS/perch-cli

  skills:
    name: Agent skills
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Both failures here are invisible at runtime, which is the whole reason
      # they are a build step. A skill with broken frontmatter installs, lists
      # and is never loaded; a stale GeneratedSkills.swift means `perch skill`
      # prints something other than the file every other consumer ships.
      - name: SKILL.md guards
        run: scripts/check-skills.sh ai perch
      - name: The embedded copy matches ai/**/SKILL.md
        run: scripts/embed-skills.sh --check

  build-ios:
    name: iOS companion build
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      # The companion shares PerchWire/ and PerchMobileCore/ with the Mac app,
      # so a change on either side can break it. Build it on every PR; the
      # signed, uploadable build lives in the testflight workflow.
      #
      # Simulator + CODE_SIGNING_ALLOWED=NO on purpose: this proves it compiles,
      # not that it runs — the App Group entitlement a real run needs is exactly
      # what unsigned builds drop.
      - name: Build PerchIOS (app + Share extension)
        run: |
          xcodebuild \
            -project Perch.xcodeproj \
            -scheme PerchIOS \
            -configuration Release \
            -destination 'generic/platform=iOS Simulator' \
            -derivedDataPath DerivedData \
            CODE_SIGNING_ALLOWED=NO \
            build
